const config = {
  theme: 'genesis',
  classes: {
    submit: {
      outer: 'mt-6',
      input: 'bg-primaryColor text-white font-bold py-2 px-4 rounded hover:bg-primaryColorHover'
    }
  }
}

export default config;
